---

- hosts: all
  roles:
    - system-initialize
    - ansible-role-firewalld
    - httpd
    #- php
    #- php55
    - php56
    - composer
    - drush
    - mariadb
    - aegir-user
    - aegir
    - aegir-hosting-queued

  environment:
    PATH: "/usr/local/bin/drush:{{ lookup('env', 'PATH') }}"
